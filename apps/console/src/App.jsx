// Placeholder M0. La consola de agentes (inbox, casos, matching,
// logística, auditoría) se construye en M7 contra /v1 con auth + MFA.
export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '4rem 1.5rem', textAlign: 'center' }}>
      <h1>Consola — Colombia Unida</h1>
      <p style={{ color: '#666' }}>Acceso restringido. En construcción.</p>
    </main>
  )
}
